{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Main",
            "cargo": {
                "args": [
                    "build",
                    "--bin=n2t",
                    "--manifest-path=${workspaceFolder}/Cargo.toml"
                ]
            },
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}